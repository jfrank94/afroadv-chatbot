# PoC Platforms RAG Chatbot - Dependencies
# Pinned versions for reproducibility. Update intentionally, not automatically.

# UI Framework
streamlit>=1.31.0,<2.0.0  # Requires 1.31.0+ for st.page_link()

# Embeddings (local)
sentence-transformers==3.4.0
torch==2.6.0  # Required by sentence-transformers

# Vector Database
qdrant-client==1.16.2

# LLM APIs
openai==1.68.2         # For Cerebras & DeepSeek (OpenAI-compatible APIs)
anthropic==0.75.0      # For Claude Haiku (primary)

# Data handling
pandas==2.2.3
openpyxl>=3.1.0,<4.0.0  # For reading Excel files

# Configuration & Environment
python-dotenv==1.2.1

# Enrichment agent
httpx==0.27.0          # HTTP client for URL checks and Tavily calls
tenacity==8.2.3        # Retry logic for flaky network calls
instructor==1.3.3      # Structured LLM output via Pydantic (wraps anthropic)

# Progress bars
tqdm==4.67.1

# Enrichment pipeline
requests==2.32.3
beautifulsoup4==4.13.3
rapidfuzz==3.12.2      # Fuzzy matching for duplicate detection

# Events pipeline
feedparser==6.0.11     # RSS/Atom feed parsing
tavily-python==0.7.17  # Web search API for event discovery
